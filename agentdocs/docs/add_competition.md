# ➕ Adding a New Competition
When attempting a new competition which is not already setup, add the competition name to the 
enum class `CompetitionID` in `third_party/ds-agent/ds_agent/competition_ids.py` and in the `ALL_COMPETITIONS_LIST` inside `third_part\ds-agent\ds_agent\competition_instances.py`.
