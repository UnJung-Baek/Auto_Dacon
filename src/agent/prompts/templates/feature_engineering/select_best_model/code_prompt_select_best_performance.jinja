{%- set task_description = memory.retrieve({memory.mem_keys.TASK_DESCRIPTION: 1.0}) %}
{%- set data_description = memory.retrieve({memory.mem_keys.DATA_DESCRIPTION: 1.0}) %}
{%- set model_perf = memory.retrieve({memory.mem_keys.MODEL_PERF: 1.0}) %}
{%- set templates_relative_path = memory.retrieve({memory.mem_keys.TEMPLATES_RELATIVE_PATH: 1.0}) %}

{% include "system_prompt.jinja" %}

[[ USER ]]

## Task description
{{ task_description }}

## Data description
{{ data_description }}

## Model performance
{{ model_perf }}

## What to do next
You ar given with a dictionary of model performances. You have to select best performance for the given data and context.
You have to select index of the best model performance from the dictionary of model performances provided and complete the code.
You do not want to do any feature engineering or model training.

Output:
Code should work without error.

Your task is to update the "best_performance_trial" variable in the code.
```python
{% include templates_relative_path + "select_best_performance_template.jinja" %}
```

Only output the code that should be filled in the blank with no extra text.Don't forget to import any additional packages that you will use in the code.
Your response should be in the format:
```python
<code>
```
