{% set summarize_cot = memory.retrieve({memory.mem_keys.SUMMARIZE_COT: 1.0}) %}
{% include "system_prompt.jinja" %}

[[ USER ]]
{% if fetched_raw_data %}
This is the raw data description from the Kaggle website:

{{ raw_data_description }}

### View of the top-level directory {{ raw_data_dir }}
after having downloaded and extracted the raw data from the source, these are the data you can use to solve this task:
```
{{ raw_data_view }}
```


Here is some information about the tables in the raw data directory
```
{{ raw_table_info }}
```

{% if has_sample_submission %}
### View of the sample submission:
The input ID column name: {{ raw_id_column_name }}
The target column name(s): {{ raw_target_column_names }}
```
{{ sample_submission_head }}
```
{% endif -%}

Now summarize the above description to extract how the different files are organized.
In particular, list all files in a sensible way and list all feature and target names relevant for solving the task.
{#Finally, present the files organization in a tree structure, following the raw data view of the top-level directory.#}
**Important**: respect scrupulously the above tree structure as it is not permitted to change the name of any file, path or directory.
**Important**: Note that the data is saved under {{ raw_data_dir }}, please reflect that in your tree structure by putting the absolute path.
{% endif -%}
{% include "summarize_cot.jinja" %}