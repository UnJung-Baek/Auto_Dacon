{% set summarize_cot = memory.retrieve({memory.mem_keys.SUMMARIZE_COT: 1.0}) %}
{% include "system_prompt.jinja" %}

[[ USER ]]
{% if fetched_raw_data %}
This is the summarized data description for the task:

{{ summarized_data_description }}

Here is some information about the tables in the raw data directory:
```
{{ raw_table_info }}
```

{% if has_sample_submission %}
### View of the sample submission:
The input ID column name: {{ raw_id_column_name }}
The target column name(s): {{ raw_target_column_names }}
```
{{ sample_submission_head }}
```
{% endif -%}

Here is a summary of the submission format for this task

{{ summarized_submission_format }}

Finally here is a roadmap of the target modalities needed for this task:

{{ summarized_targets_modality_maps }}

Your task is now to determine which target transforms are needed for the modalities of this task.
These transforms are functions that are applied to the targets to transform them from their original format to one that is usable by a machine learning model.
The inverse transform does the reverse, enabling target predictions to be transformed back into the expected submission format.

For example, in an image multiclass classification task, we would have:

    - tabular target transform: YES
    the `transform` function needs to convert class labels into one hot encodings
    the `inverse_transform` function needs to convert the one hot encoded classes back into the submission format
    - image target transform: NO
    there are no image targets in this task
    - text target transform: NO
    there is no presence of text modality in this task

Following the structure in the above example, please make a list of all the target transforms needed for the task and their description, do not write code yet.

{% endif -%}
{% include "summarize_cot.jinja" %}