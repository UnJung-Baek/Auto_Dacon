{%- set task_description = memory.retrieve({memory.mem_keys.TASK_DESCRIPTION: 1.0}) %}
{%- set data_description = memory.retrieve({memory.mem_keys.DATA_DESCRIPTION: 1.0}) %}
{%- set available_actions = memory.retrieve({memory.mem_keys.AVAILABLE_ACTIONS: 1.0}) %}
{%- set code = memory.retrieve({memory.mem_keys.CODE: 1.0}) %}
{%- set code_output = memory.retrieve({memory.mem_keys.CODE_OUTPUT: 1.0}) %}
{%- set code_error = memory.retrieve({memory.mem_keys.CODE_ERROR: 1.0}) %}
{% set summarize_cot = memory.retrieve({memory.mem_keys.SUMMARIZE_COT: 1.0}) %}

{% include "system_prompt.jinja" %}

[[ USER ]]

## Task description
{{ task_description }}

## Data description
{{ data_description }}

## Available solutions
{%- include "./top_level/submission_list.jinja" %}

## What to do next
You have to generate a code to bag the above submisssions to improve the overall performance through some function.
You need to write a function such as mean for regression or majority vote for classification tasks which will pick the most likely solution in each row of the final submission files.
You can choose a relevant function which will maximise the accuracy of your predictions.

{% include "./ds_main_pipeline/code_error_report_think.jinja" %}

Output a plan presented with maximum 4 bullet points describing the different steps to construct a function.
The plan should be tailored to the current task as it will be used to guide the implementation of the bagging function.
{% include "./ds_main_pipeline/plan_cot.jinja" %}